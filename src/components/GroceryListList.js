const GroceryListList = (props) => {
  return (
    <>
      <div>{props.componentsGrocery}</div>
      <button>add entire list to the food list</button>
    </>
  );
};

export default GroceryListList;
